<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.awall.awalladmin.dao.TrainMapper">
    
    <select id="queryTrainById" parameterType="String" resultType="cn.awall.awalladmin.pojo.Train">
        select * from tb_train where id = #{id}
    </select>
    
</mapper>

